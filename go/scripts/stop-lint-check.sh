#!/bin/bash
# Stop hook: Placeholder for lint-before-stop functionality
#
# Currently disabled because the Stop hook doesn't receive context
# about which project to lint. Will be implemented when we have
# a way to track the current project context.
#
# For now, just allow stopping without lint check.

exit 0
