package main

import (
	"context"
	"fmt"

	"github.com/spf13/cobra"
)

func {{.CmdName}}Cmd() *cobra.Command {
	return &cobra.Command{
		Use:   "{{.CmdName}}",
		Short: "{{.Description}}",
		RunE: func(cmd *cobra.Command, args []string) error {
			return run{{.CmdNameTitle}}(cmd.Context())
		},
	}
}

func run{{.CmdNameTitle}}(ctx context.Context) error {
	cfg, err := LoadConfig()
	if err != nil {
		return fmt.Errorf("failed to load config: %w", err)
	}

	// TODO: Implement {{.CmdName}} logic
	_ = cfg
	return nil
}
