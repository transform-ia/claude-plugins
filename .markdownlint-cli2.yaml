---
# Disable rules that are too strict for plugin documentation

# Allow emphasis as headings (common in structured docs)
MD036: false

# Allow code blocks without language (many are pseudo-code or output)
MD040: false

# Allow multiple top-level headings (multi-document files)
MD025: false

# Allow duplicate headings (common in structured docs)
MD024: false
