---
description: "Preview rendered templates: /helm:template [directory] [release-name]"
allowed-tools: [Bash]
---
Run helm template to preview rendered Kubernetes manifests.

```
Bash("${CLAUDE_PLUGIN_ROOT}/scripts/template-exec.sh $ARGUMENTS")
```
